import { FC } from 'react';
import styles from './Footer.module.scss';

export const Footer:FC =()=> {
  return(
  <footer className={styles.footer}>
    <h1 className={styles.footerText}>create for test API</h1>
  </footer>) ;
}
